<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.windowshowdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".popup.PopupWindowActivity"></activity>
        <activity
            android:name=".dialog.DialogActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".loading.StaticLoadActivity" />
        <activity android:name=".delay.DelayDialogActivity" />
        <activity
            android:name=".xianyu.PubActivity"
            android:theme="@style/PubTheme" />
        <activity android:name=".LikeXianYuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PopNiceActivity" />
        <activity
            android:name=".TopViewActivity"
            android:theme="@style/TopViewTheme" />
    </application>

</manifest>